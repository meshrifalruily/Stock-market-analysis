# منهجية النمذجة والتقييم (تحديث الحزم A + B + C + D)

هذا المستند يوثّق التحسينات المنهجية التي أُجريت على نظام التنبؤ، وأهم
**النتيجة الصادقة** التي كشفها التقييم الصارم.

---

## ⚠️ الخلاصة الأهم: النماذج الحالية بلا حافة تنبؤية مؤكدة

بعد إصلاح تسريب البيانات واعتماد مقاييس صادقة، تبيّن أن:

| المقياس | القيمة | الدلالة |
|---------|--------|---------|
| دقة اتجاه الانحدار | ~52% | بالكاد فوق رمي العملة (50%) |
| IC للمصنّفات | **−0.03 إلى −0.06** | حافة سلبية (أسوأ من العشوائي) |
| Walk-Forward IC | −0.017 ± 0.033 | غير ثابت؛ 25% فقط من النوافذ موجبة |
| الباك تست (سنة) | **−2.61%**، شارب −0.43 | دون مؤشر تاسي |
| price r² | 0.999 | **وهم** — إغلاق الغد ≈ إغلاق اليوم |

**هذا ليس عيباً في الكود بل نتيجة حقيقية**: المقاييس القديمة (r² على السعر،
و`strategy_total_return=0` الذي يسقط إلى `auc*10`) صنعت وهم نجاح. التقييم
الصارم أزال هذا الوهم.

> **توصية مهنية:** لا تُعتمد التوصيات الحالية مالياً. النظام الآن صادق في
> قياس نفسه — وهذا شرط أساسي قبل أي تطوير لاحق أو مخاطرة برأس مال.

---

## ما الذي تغيّر؟

### الحزمة A — إصلاحات حرجة لصحة التنبؤ
- **A1 — مقاييس العائد الحقيقية:** `evaluate_regressor` يعتمد الآن على
  Information Coefficient (Spearman) + دقة الاتجاه + MAE على *العائد*، بدل
  r²/MAPE على *السعر* المضلّلين (يُحتفظ بهما للمرجع فقط مع وسم صريح).
- **A2 — تقييم استراتيجية صادق:** أُضيف مقياس عائد *الخُمس الأعلى* و
  *long-short spread* و IC يومي — تُنتج إشارة دائماً بدل `selected_days=0`.
  معيار اختيار النموذج صار: `spread*200 + IC*50 + AUC*10 + strategy*20`.
- **A3 — فجوة embargo:** `chronological_split` يحذف آخر 10 أيام من التدريب
  (= أقصى أفق نظر للأهداف) لمنع تسرّب نوافذ الأهداف إلى فترة التحقق.

### الحزمة B — صرامة التقييم
- **B1 — معايرة الاحتمالات:** المصنّفات الثلاثة مغلّفة بـ
  `CalibratedClassifierCV` (isotonic, prefit عبر `FrozenEstimator`) لتصبح
  `p_weekly` احتمالات حقيقية موثوقة للعتبات.
- **B2 — Walk-Forward متعدد النوافذ:** تحقق عبر 4 نوافذ متتابعة مع
  purge/embargo لقياس ثبات الحافة عبر الزمن (يُحفظ في `model_metrics.json`).
- **B3 — باك تست واقعي:** الفترة قابلة للتمديد (`TASI_BACKTEST_DAYS`، افتراضي
  365 يوماً)، والمعيار المرجعي صار **مؤشر تاسي الفعلي** بدل سهم الراجحي منفرداً.

### الحزمة C — البيانات والميزات
- **C1 — مشاعر عربية حقيقية:** `scripts/sentiment.py` — مُصنّف معجمي مالي
  عربي يعمل دون اتصال + خطّاف اختياري لنموذج محوّلات (CAMeLBERT) + واجهة
  `fetch_headlines` تُوصَل بمصدر أخبار حقيقي. يستبدل قيمة `sentiment` الثابتة.
- **C2 — ميزات microstructure:** أُضيفت 7 ميزات (موضع الإغلاق داخل المدى،
  فجوة الافتتاح، المدى النسبي، عائد جلسة، الظلال، متوسط CLV). لم تُحسّن IC،
  لكنها توسّع فضاء البحث عن إشارة.

### الحزمة D — قياس عدم اليقين
- **D2 — Conformal Prediction:** تُحسب كميات المتبقيات على عيّنة معايرة زمنية
  وتُحفظ في `models/regression_conformal.joblib`. التطبيق يعرض فترة ثقة 80%
  للسعر المتوقع: `[close*(1+r−q̂), close*(1+r+q̂)]` بدل رقم نقطي.

---

---

## بحث الإشارة (تحديث لاحق): اكتشاف الانعكاس + اختبار الواقعية

طُبّق منهج بحث IC للعوامل (`scripts/signal_research.py`) عبر 750 يوماً و6 آفاق:

### النتيجة 1 — السوق انعكاسي قصير المدى (دلالة ساحقة)
- `close_location_value` أفق يوم: **t = −21.8** (العتبة 2)، IC=−0.083، ثبات 78%.
- **كل أقوى 20 إشارة انعكاسية** (سالبة): العائد/CLV/الظلال/rsi_slope.
- يفسّر تماماً لماذا كانت نماذج الزخم القديمة ذات IC سالب: راهنت عكس السوق.

### النتيجة 2 — الانعكاس الصرف long-only يخسر بعد التكاليف
شراء المتشبّع بيعياً وحده: −27% إلى −56%، لأن الربح كان في ساق البيع المكشوف
(غير متاحة بتاسي) وأفق اليوم تأكله العمولات (~0.41% ذهاب/إياب).

### النتيجة 3 — "شراء الانخفاض في اتجاه صاعد" (مثالي vs واقعي)
الدمج المتحقّق منه (`scripts/validate_reversal.py`): زخم متوسط (close>sma_50,
momentum_20d>0) + دخول عند تراجع قصير (أعلى درجة انعكاس)، أعلى 10%، أفق 5 أيام:
- **مثالي (بلا وقف، متنوّع):** +32.9% مقابل سوق −19.3%، ألفا موجب 3 من 4 سنوات، دفاعي.
- **واقعي (باك تست بعمولات/أوقاف/تدوير يومي، 3 سنوات):** **−18.3%، شارب −0.87**.

> **الخلاصة الصادقة النهائية:** الإشارة الانعكاسية **حقيقية إحصائياً** وأفضل تأسيساً
> من الزخم القديم، لكنها **لا تُنتج بعد باك تستاً واقعياً رابحاً** على النافذة المتاحة
> (المهيمَن عليها بهبوط 2024–2025). الفجوة بين المثالي والواقعي = احتكاك التنفيذ
> (تكاليف، أوقاف، تركيز، نافذة قصيرة). **لا يُعتمد النظام مالياً.**

### النتيجة 4 — تأكيد على 8 سنوات: بنية مزدوجة (انعكاس + زخم)
بعد جلب تاريخ أطول (2018–2026، 8 سنوات، 413 ألف صف) وإعادة بحث IC، تأكّدت
بنية كلاسيكية مزدوجة في تاسي:
- **الأفق القصير (1–5 أيام): انعكاس** — lower_shadow_pct, close_location_value,
  return_2d (IC سالب، t حتى −24). مستقر عبر الأنظمة.
- **الأفق الطويل (20–60 يوماً): زخم** — `price_vs_sma_200` أفق 60ي: **t=+27.2**
  (أقوى إشارة على الإطلاق، IC=+0.095، شارب long-short ≈ 7.9 إجمالي).

هذا يؤكّد مزدوجاً منطق "شراء الانخفاض في اتجاه صاعد": الزخم الطويل يحدّد *ماذا*
نملك (الإشارة الموجبة)، والانعكاس القصير يحدّد *متى* ندخل (الإشارة السالبة).

## ما طُبّق على الكود
- `reversal_score` (درجة انعكاس مركّبة) في preprocess + كميزة نموذج.
- `add_hybrid_scores` و`entry_candidates`: منطق "شراء الانخفاض في صعود" بدل مطاردة الزخم.
- `build_trade_setup` (الواجهة): بوابة انعكاسية متّسقة، ترتيب الدخول ضمن العالم
  الصاعد المؤهّل، وأُزيلت عتبة الاحتمال المطلقة (عطّلتها المعايرة + IC السالب).
- باك تست: خروج زمني/مخاطر (أُزيل البيع المبكر المبني على احتمال النموذج السالب).
- إعدادات ملائمة للانعكاس: وقف أوسع (2.5 ATR، يتجنّب الـ whipsaw)، حجز 5 أيام.

### النتيجة 5 — استراتيجية الزخم طويلة الأفق: الحافة الرابحة ✅
بناءً على إشارة الزخم الأقوى (price_vs_sma_200، t=+27)، بُنيت ودُرِّبت واختُبرت
استراتيجية زخم long-only (`scripts/momentum_strategy.py` + `validate_momentum.py`):
- درجة زخم مركّبة من 6 عوامل IC موجبة، أفق 60 يوماً، أعلى 10%، فلتر نظام، صافي تكاليف.
- **النتيجة على 8–9 سنوات: +149% مقابل سوق +36%، Alpha-Sharpe 0.80، ألفا موجب في 8/9 سنوات** (حتى 2025 الهابطة).
- نجح حيث فشل الانعكاس لأن الأفق الطويل = تدوير منخفض = تكاليف مهملة، والزخم
  يعمل long-only بلا بيع مكشوف.
- طُبّق في التطبيق: صفحة `/momentum` تعرض أقوى الأسهم زخماً + إحصاءات التحقّق.

> **الخلاصة المحدّثة:** بعد رحلة بحثية كاملة (إصلاح المقاييس → اكتشاف الانعكاس →
> فشله بعد التكاليف → اكتشاف الزخم الطويل) وصلنا إلى **حافة قابلة للاستغلال
> ومتحقّق منها**: الزخم طويل الأفق. تبقى أداة تحليلية تعليمية لا توصية مالية،
> والأداء التاريخي لا يضمن المستقبل.

## الخطوات القادمة المقترحة
1. **تاريخ أطول وأنظمة متعددة:** النافذة 3 سنوات قصيرة؛ بيانات أقدم قد تكشف ثبات الألفا.
2. **خروج أذكى للانعكاس:** هدف ربح عند العودة للوسط (mean) بدل ATR ثابت.
3. **فلتر نظام أدق:** تفعيل الاستراتيجية فقط في أسواق متذبذبة (لا هابطة بقوة).
4. **مشاعر أخبار فعلية:** وصل `scripts/sentiment.py` بمصدر حقيقي واختبار IC.
5. **الاعتراف بالحدود:** حتى تثبت الربحية واقعياً، يُعرض النظام كأداة فرز/تعليم لا توصية.
